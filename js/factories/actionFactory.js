'use strict';

/*
angular.factory('createAction', function () {
  var ActionViewModel = function(title, content, note, classes) {
    if (!(this instanceof ActionViewModel)) {
      return new ActionViewModel(type, url);
    }

    this.type = type;
    this.url = url;

  };

  return ActionViewModel;
});
*/